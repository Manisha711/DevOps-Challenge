apiVersion: apps/v1
kind: Deployment
metadata:
  name: tb-platform
spec:
  template:
    spec:
      containers:
      - name: tb-platform
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "400m"
            memory: "512Mi"